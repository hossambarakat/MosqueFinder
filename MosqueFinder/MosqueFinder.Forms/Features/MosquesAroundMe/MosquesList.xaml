<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Mosques Around Me"
             xmlns:local="clr-namespace:MosqueFinder.Forms.Converters;assembly=MosqueFinder.Forms"
             x:Class="MosqueFinder.Forms.Features.MosquesAroundMe.MosquesList"

             >

  <ContentPage.Resources>
    <ResourceDictionary>
      <local:BooleanToStringConverter x:Key="BooleanToStringConverter" />
    </ResourceDictionary>
  </ContentPage.Resources>

  <ListView x:Name="mosquesListView" ItemsSource="{Binding Mosques}" HasUnevenRows="true" BackgroundColor="#ecf0f1">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell Height="150">
          <ViewCell.View>

            <StackLayout Spacing="2" BackgroundColor="White" VerticalOptions="StartAndExpand" Orientation="Vertical" Padding="10, 10, 10, 10">

                <Frame HeightRequest="150" BackgroundColor="White" VerticalOptions="FillAndExpand" Padding="5, 5, 5, 5">

                <Grid HorizontalOptions="Fill" RowSpacing="2">
                	<Grid.ColumnDefinitions>
                		<ColumnDefinition Width="Auto"></ColumnDefinition>
                		<ColumnDefinition Width="*"></ColumnDefinition>
                	</Grid.ColumnDefinitions>
					<Image Source="verified.jpg" Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" HorizontalOptions="End"></Image>
					<Label Text="{Binding Name}" Grid.Row="0" Grid.Column="0"  FontSize="10" />

                  	<Label Text="{Binding Address1}" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" FontSize="10" />
                  	<Label Text="{Binding Address2}"  Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" FontSize="10"/>
                  	<Label Text="Weekdays:" Grid.Row="3" Grid.Column="0"  FontSize="10"/>
                  	<Label Text="{Binding Weekdays}" Grid.Row="3" Grid.Column="1" FontSize="10" />

                  	<Label Text="Saturday:" Grid.Row="4" Grid.Column="0" FontSize="10"/>
                  	<Label Text="{Binding Saturady}" Grid.Row="4" Grid.Column="1" FontSize="10" />

                  	<Label Text="Sunday:" Grid.Row="5" Grid.Column="0" FontSize="10"/>
                  	<Label Text="{Binding Sunday}" Grid.Row="5" Grid.Column="1" FontSize="10" />

                  	<Label Text="Saturday:" Grid.Row="6" Grid.Column="0" FontSize="10"/>
                  	<Label Text="{Binding Jumma, Converter={StaticResource BooleanToStringConverter}}" Grid.Row="6" Grid.Column="1"  FontSize="10"/>  

                  	<Label Text="Ladies Room:" Grid.Row="7" Grid.Column="0" FontSize="10"/>
                  	<Label Text="{Binding LadiesRoom, Converter={StaticResource BooleanToStringConverter}}" Grid.Row="7" Grid.Column="1"  FontSize="10"/>

                  	<Button Text="View on Map" HorizontalOptions="End" Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2" ></Button>
                </Grid>

               
          	</Frame>
            </StackLayout>

          </ViewCell.View>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>